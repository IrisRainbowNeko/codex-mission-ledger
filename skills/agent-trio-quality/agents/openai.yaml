interface:
  display_name: "Agent Trio Quality"
  short_description: "Run one quality-first Agent Trio task"
  default_prompt: "Use $agent-trio-quality to complete this task with quality-first multi-agent execution."

dependencies:
  tools:
    - type: "mcp"
      value: "agent_trio"
      description: "Agent Trio V3 runtime"
      transport: "stdio"

policy:
  allow_implicit_invocation: false
