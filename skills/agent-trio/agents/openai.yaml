interface:
  display_name: "Agent Trio"
  short_description: "Run cost-aware parallel work with Agent Trio"
  default_prompt: "Use $agent-trio to complete this task with cost-aware multi-agent execution."

dependencies:
  tools:
    - type: "mcp"
      value: "agent_trio"
      description: "Agent Trio V3 runtime"
      transport: "stdio"

policy:
  allow_implicit_invocation: false
