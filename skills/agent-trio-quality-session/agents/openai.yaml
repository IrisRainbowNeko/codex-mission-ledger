interface:
  display_name: "Agent Trio Quality Session"
  short_description: "Keep follow-ups quality-first"
  default_prompt: "Use $agent-trio-quality-session to start a continuing quality-first Agent Trio task."

dependencies:
  tools:
    - type: "mcp"
      value: "agent_trio"
      description: "Agent Trio V3 runtime"
      transport: "stdio"

policy:
  allow_implicit_invocation: true
